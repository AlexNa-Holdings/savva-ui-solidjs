# SAVVA Platform - Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# Git Branches (optional - have sensible defaults)
# =============================================================================

GIT_MAIN_BRANCH=main          # Main development branch (default: main)
PROD_BRANCH=prod              # Production release branch (default: Prod)

# =============================================================================
# Deployment via SSH (optional - for automated deployment)
# =============================================================================
# Configure these to enable `npm run release` deployment

DEPLOY_HOST=                  # SSH host, e.g. your-server.com or SSH config alias
DEPLOY_USER=                  # SSH username on the server
DEPLOY_PATH=                  # Remote directory path, e.g. /var/www/savva

# Optional deployment settings
DEPLOY_PORT=22                # SSH port (default: 22)
DEPLOY_SSH_KEY=               # Absolute path to SSH private key (optional)

# =============================================================================
# OpenAI API (optional - for Claude Code / AI development tools)
# =============================================================================
# These are NOT used by the app itself - only by Claude Code for development assistance
# The app's AI features use browser localStorage settings configured in Settings > AI

# OPENAI_API_KEY_PATH=/path/to/openai/key.txt   # Path to file containing API key
# OPENAI_API_BASE=https://api.openai.com/v1    # API base URL
# OPENAI_MODEL=gpt-4o-mini                      # Model to use
# OPENAI_TEMPERATURE=1                          # Temperature (0-2)

# =============================================================================
# Notes
# =============================================================================
# - The app does NOT require any environment variables to run
# - All app configuration comes from the blockchain Config contract
# - User's AI settings (API keys, models) are stored in browser localStorage
# - Never commit real API keys or secrets to version control
