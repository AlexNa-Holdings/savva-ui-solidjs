# SAVVA Platform - Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# Git & Deployment Configuration
# =============================================================================

# Git branches (optional; these have defaults)
GIT_MAIN_BRANCH=main          # default: main
PROD_BRANCH=Prod              # default: Prod

# Deployment via SSH (required to enable automated deployment)
# Leave empty to skip deployment
DEPLOY_HOST=                  # e.g. your-server.com
DEPLOY_USER=                  # SSH username
DEPLOY_PATH=                  # Remote directory path, e.g. /var/www/app

# Optional deployment settings
DEPLOY_PORT=22                # SSH port (default: 22)
DEPLOY_SSH_KEY=               # Absolute path to SSH private key (optional)

# =============================================================================
# Application Configuration
# =============================================================================

# Website URL
VITE_WEBSITE_URL=https://savva.app

# Backend API endpoint
# VITE_BACKEND_URL=https://api.savva.app

# IPFS Gateway
# VITE_IPFS_GATEWAY=https://ipfs.savva.app

# =============================================================================
# Development Tools (Optional)
# =============================================================================

# OpenAI API (for development tools)
# OPENAI_API_KEY_PATH=/path/to/openai/key.txt
# OPENAI_API_BASE=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4
# OPENAI_TEMPERATURE=0.7

# =============================================================================
# Notes
# =============================================================================
# - All VITE_ prefixed variables are exposed to the client-side code
# - Never commit real API keys or secrets to version control
# - Keep your .env file in .gitignore
